#include <stdio.h>

#include "controller.h"
#include "view/view.h"


void controller_process_msg(view_controller_command_t msg, model_t *model) {
    switch (msg.code) {

        default:
            break;
    }
}